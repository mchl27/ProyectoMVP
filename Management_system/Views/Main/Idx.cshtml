@{
    ViewData["Title"] = "Inicio Principal";
    Layout = "~/Views/Shared/_Layout_main.cshtml";
}

<style>
    .contenedor-bienvenida button {
        background-color: #0f268d;
        color: #fff;
    }

        .contenedor-bienvenida button:hover {
            background-color: #0f268d;
            color: #fff;
        }

    .card {
        border-radius: 15px;
    }

</style>

@* NAVIDAD TEMA *@
<div>
    @* <style>
    .snowflake {
    color: #87CEEB;
    font-family: sans-serif;
    font-size: 1.5em;
    position: fixed;
    top: -2em;
    z-index: 1000;
    user-select: none;
    pointer-events: none;
    animation: fall linear infinite;
    }

    @@keyframes fall {
    0% {
    transform: translateY(0) rotate(0deg);
    opacity: 1;
    }

    100% {
    transform: translateY(100vh) rotate(360deg);
    opacity: 0.2;
    }
    }
    </style>

    <script>
    document.addEventListener("DOMContentLoaded", function () {
    const snowflakeCount = 50;

    for (let i = 0; i < snowflakeCount; i++) {
    let snowflake = document.createElement("div");
    snowflake.className = "snowflake";
    snowflake.innerHTML = "❄";

    snowflake.style.left = Math.random() * 100 + "vw";
    snowflake.style.animationDuration = Math.random() * 5 + 5 + "s";
    snowflake.style.fontSize = Math.random() * 10 + 10 + "px";

    document.body.appendChild(snowflake);
    }
    });
    </script> *@
</div>


<div class="container mt-5">
    <!-- Contenedor principal con texto e imagen -->
    <div class="row align-items-center mb-5">
        <div class="col-md-6">
            <P class="display-5">
                Bienvenido/a @User.Identity.Name
            </P>
            <p>¡Sistema de gestión de nuestra empresa!</p>
            <p class="lead">Este sistema está diseñado para facilitar tus tareas diarias y mejorar la eficiencia en el trabajo.</p>
            @* <a href="@Url.Action("Login", "Home")" class="btn btn-primary btn-lg mt-3">Ingresar</a> *@
        </div>

        <div class="col-md-6 text-center">
            @* <img src="img/2. Fachada (2).jpeg" alt="Gestión Empresarial" class="img-fluid rounded" width="490" height="390"> *@
            <img src="@Url.Content("~/img/Terminator.jpeg")" alt="Gestión Empresarial" class="img-fluid rounded" width="420" height="390">

        </div>

    </div>
</div>
